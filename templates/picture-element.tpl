<div data-category="{{ category_name }}, {{ image_year }}" class="work">
     <a rel="work" title="{{ image_title }}  :  {{ image_spec }} ({{ image_year }}) " href="{{ image_url }}">
         <img src="{{ image_thumbnail_url }}" title ="{{ image_title }} ({{ image_year }})" />
     </a>
 </div>